create database if not exists ikman;

use ikman;

create table Adds (
    district varchar (255),
    category varchar (255),
    model varchar (255),
    price varchar (255),
    descr varchar (255)

)ENGINE=INNODB;